# System properties to set for loadtest run:
* petclinic.path (e.g. /home/user/workspace/spring-petclinic, no trailing slash)
* stagemonitor.version (e.g. 0.75 - just for reporting)

jmeter has to be available in $PATH
